
# BLOGS
#### BLOGS for writing blog created using React

# Getting Started
### To get run the server locally
##### -clone this repo
##### -npm install to install all required dependencies
##### - run the command npm json-server --watch data/db.json --port 8000 to start thr local server
##### -to start npm start
